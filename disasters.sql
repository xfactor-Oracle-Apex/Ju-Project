
  CREATE TABLE "DISASTERS" 
   (	"DISASTER_ID" NUMBER GENERATED BY DEFAULT AS IDENTITY MINVALUE 1 MAXVALUE 9999999999999999999999999999 INCREMENT BY 1 START WITH 1 CACHE 20 NOORDER  NOCYCLE  NOKEEP  NOSCALE  NOT NULL ENABLE, 
	"NAME" VARCHAR2(100) NOT NULL ENABLE, 
	"NAME_BN" VARCHAR2(150) NOT NULL ENABLE, 
	"DISASTER_TYPE_ID" NUMBER NOT NULL ENABLE, 
	"AFFECTED_DIVISION" NUMBER, 
	"AFFECTED_DISTRICT" NUMBER, 
	"START_DATE" DATE NOT NULL ENABLE, 
	"END_DATE" DATE, 
	"STATUS" VARCHAR2(30), 
	 PRIMARY KEY ("DISASTER_ID")
  USING INDEX  ENABLE
   ) ;

  ALTER TABLE "DISASTERS" ADD CONSTRAINT "FK_DISASTER_DIST_ID" FOREIGN KEY ("AFFECTED_DISTRICT")
	  REFERENCES "BD_DISTRICTS" ("ID") ENABLE;
  ALTER TABLE "DISASTERS" ADD CONSTRAINT "FK_DISASTER_DIV_ID" FOREIGN KEY ("AFFECTED_DIVISION")
	  REFERENCES "BD_DIVISIONS" ("ID") ENABLE;
  ALTER TABLE "DISASTERS" ADD CONSTRAINT "FK_DISASTER_TYPE" FOREIGN KEY ("DISASTER_TYPE_ID")
	  REFERENCES "DISASTER_TYPES" ("TYPE_ID") ENABLE;